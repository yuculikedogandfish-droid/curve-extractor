# Curve Extractor — 版本台账与回滚

> **当前开发分支**：`dev`（日常提交都推这里）  
> **当前线上**：`master` → GitHub Pages  
> **当前版本**：v1.4.19（commit `d624bf5`）  
> **线上地址**：https://yuculikedogandfish-droid.github.io/curve-extractor/app.html  
> **仓库**：https://github.com/yuculikedogandfish-droid/curve-extractor  
> **更新日期**：2026-09-01

本文给后续 Agent 和同事用：先看「怎么开发 / 怎么回滚」，再按下表对版本。详细模块说明见 [HANDOFF.md](./HANDOFF.md)。

---

## 分支约定

| 分支 | 用途 | 谁能推 |
|------|------|--------|
| `dev` | 开发。每一小步功能都 commit + push 到这里 | 日常开发 |
| `master` | **线上**。GitHub Pages 只部署这个分支 | 明确说「上线」才合并 |

**不要**在 `master` 上直接改算法。流程：

```bash
# 开发
git checkout dest
# …改 app.html…
git add app.html
git commit -m "v1.4.xx: 一句话说明为什么改"
git push origin dest

# 上线（产品确认后再做）
git checkout master
git merge dest
git push origin master
git checkout dest
```

唯一需要改的源文件是 **`app.html`**（单文件前端）。不要拆成多文件。

---

## 怎么回滚（不改写历史，推荐）

线上或本地回到某一版，只换 `app.html`，留一条新 commit：

```bash
git checkout dest          # 或 master（若要回滚线上）
git checkout v1.4.18 -- app.html
git add app.html
git commit -m "rollback app.html to v1.4.18"
git push origin HEAD
```

若要回滚 **线上**，在 `master` 上做同样操作并 `git push origin master`。

对照 tag 列表：`git tag -l "v1.4*"`  
对照某版代码：`git show v1.4.16:app.html`

---

## 测试素材（不进 git）

`input/tri_labeled/` 下调试图在 `.gitignore` 里。本地应有：

```
input/tri_labeled/processed_nolabel/group1_{front,side,top}.png
input/tri_labeled/debug/verify_overlay.py   # Playwright 贴墨率
```

判定：正视黄线贴正面墨水；侧视贴侧面墨水。「信顶面=0」时俯视可以对不齐（三张手绘不是同一条空间线）。

```bash
cd curve-extractor
python input/tri_labeled/debug/verify_overlay.py
```

---

## 版本总表

| Tag | Commit | 一句话 | 建议 |
|-----|--------|--------|------|
| v0.8 / v0.9 | 见 git | 早期方向场 / Pages | 考古用 |
| v1.0 | `4c90884` | 三视图 + 去背景 + UV 面片 | 基线 |
| v1.1 | `6dc248e` | 面片居中、3D 编辑、FBX | 光效图算法较稳 |
| v1.2 | `47836c1` | 三视图拖拽、Otsu | 单图曾退化 |
| v1.3 | `df9834c` | 自动追踪模式 | 单图曾退化 |
| v1.4 | `1a42f97` | **回滚算法到 v1.1**，保留拖拽 | 光效图稳定锚点 |
| v1.4.1–.11 | 见下 | 线稿三视图探索 | 中间态，勿当线上 |
| **v1.4.16** | `996775a` | 线稿不 B 样条超调；侧视 Z 沿轮廓 | 可回滚点 |
| **v1.4.17** | `0a9e64c` | 线稿按走向惯性过交叉；点选补一笔 | 交叉口打结明显改善 |
| **v1.4.18** | `83d074b` | 侧视 Z 沿墨水连续走，交叉口不再出台阶 | 侧视贴墨约 98% |
| **v1.4.19** | `d624bf5` | **当前**：墨水包围盒对齐 +「信顶面」滑条 | **线上目标** |

---

## v1.0 – v1.4（光效图 / 产品骨架）

### v1.0 `4c90884`
- 三视图识别、自动去背景、UV 面片、基础 UX。

### v1.1 `6dc248e`
- 面片以曲线为轴、3D 预览可编辑、FBX 导出。
- **光效图（黑底金带）仍以这套形态学 + 方向场为主。**

### v1.2 `47836c1` / v1.3 `df9834c`
- 拖拽三视图、Otsu、自动追踪。
- **问题**：单图识别变碎、变差。用户要求回滚算法。

### v1.4 `1a42f97`
- 算法回到 v1.1，保留 v1.2 拖拽 UI。
- 之后所有 `v1.4.x` 都在这条线上小步迭代，**没有再整包回滚**。

---

## v1.4.1 – v1.4.19（线稿三视图，按时间）

测试对象主要是白线黑底手绘三视图（group1：正面 / 侧面 / 顶面，约 1024²）。用户要的是：3D 黄线在正交正/侧/俯视下叠在参照图墨水上，交叉口按走向穿过而不是拐成圈。

### v1.4.1 `d895ec7`
四端短段连接，可退回 v1.1 连接逻辑。

### v1.4.2 `70fae37`
白线黑底线稿增强（放宽亮度、先闭运算）。

### v1.4.3 `c072a40` / v1.4.4 `23636e4`
沿骨架走完整路径，减轻 3D 涂鸦；线稿走满骨架以免笔画被截断。

### v1.4.5 `19ba2ac`
正/侧笔画配对做深度，不再用整行中位数。

### v1.4.6 `719a560`
对极几何取 Z；去背景预览与正面图同步；3D 参照图画在 z=0 平面。

### v1.4.7 `7f11fd2`
叠笔合并；双曲面 3D；恢复面片；参照图更清晰。

### v1.4.8 `de8b4d5` / v1.4.9 `764da9b`
正交正/侧/俯相机，三向对照参照图。

### v1.4.10 `9dd987e` / v1.4.11 `f850ce0`
侧/顶参照与 Z 用同一套 0–1 映射；参照图按像素 blit，避免黄线与墨水错位。

### v1.4.12 `c6c33a4`
Z 取侧面骨架最长路径，让侧视黄线落在侧面墨水上。  
**问题遗留**：最长路径经过交叉口会走出 **台阶 / 方框**。

### v1.4.13 `b486268`
`traceStrokesHuman`：交点穿过分叉，减少假闭环。  
**问题遗留**：骨架图在交叉口仍会打结；用户图是多笔叠画。

### v1.4.14 `c8fa9a2`
正交预览只画中心线，不画面片线框（避免「线窝」）。

### v1.4.15 `9dd503a`
线稿全部亮黄显示；放宽叠笔合并，避免把不同枝并成一根。

### v1.4.16 `996775a`
线稿不做 B 样条（防超调），改 RDP + 重采样；侧视 Z 仍偏轮廓路径。  
**现象**：约 9 根曲线；交叉口打结；侧视台阶；俯视方框。

### v1.4.17 `0a9e64c`
- 线稿改为在 **原图墨水** 上按方向场 + **惯性** 过交叉口，不再沿 1px 骨架图走。
- 「点选补一笔」：到「曲线」页点漏掉的线。
- group1：约 **3 根**、0 闭环、正视贴墨 ~99.5%。
- **问题**：点选补成 4 根后更乱；侧视仍可能出台阶（多条正面线共用带拐角的侧面深度）。

### v1.4.18 `83d074b`
- 侧视 Z：同一高度在侧面墨水上取候选，沿来时方向连续走，**不拐进交叉口另一枝**。
- 重复点选会提示「这一笔已经有了」。
- group1：侧视贴墨 ~**98.4%**，正视 ~99.5%。
- **未解决**：俯视叠不上——正+侧已定 XYZ，手绘顶图不是同一条空间线。

### v1.4.19 `d624bf5`（当前）
- **按墨水包围盒对齐**三张图（消留白/缩放差）。
- **「信顶面」0–100%**：深度在侧面与顶面之间折中。
  - 0%：侧视 ~98%，俯视 ~16%
  - 100%：俯视 ~99%，侧视 ~18%
- **几何事实**：XY 锁在正面时，Z 不能同时满足侧视和俯视。形状本身画得不一致时，滑条是产品方案，不是「三张同时贴死」。

---

## 当前仍存在的问题（交接必读）

1. **三张手绘不一致**：不能指望自动让黄线同时叠在正/侧/顶三张墨水上。用「信顶面」选更在意的一视；要三张都准只能重画顶图或侧图。
2. **线稿是多笔叠画 + X/T 交叉**：自动追踪可能漏笔或在 T 口拐错。缺笔用「点选补一笔」，不要对已有笔画连点。
3. **精细模式 / 背景容差**：对白线黑底线稿几乎无用；那是给杂色底光效图的。
4. **面片无贴图**：UV 有，贴图在 UE 里做。
5. **HANDOFF.md 里旧的行号会漂**：以函数名为准，用 Grep。

---

## 关键代码入口（v1.4.19）

| 主题 | 函数 / 状态 |
|------|-------------|
| 总管线 | `extractAll()` |
| 线稿检测 | `S.lineDrawing`、`detectLineDrawing()` |
| 线稿追踪 | `extractCurvesOrientationField()` → `traceOrientationField()`（惯性 + 前瞻） |
| 点选补笔 | `pickStrokeAt()`、`#btnPickStroke` |
| 三视图深度 | `processTriViews()`、`reconstructTriView()`、`S.triTrustTop`、`S.triAlignInk` |
| 3D 与导出同源 | `buildCurveWorldPoints()` |
| 正交对照 | `drawOrthoBlueprint()`，`S.view3d.preset` = front/side/top |
| 面片 | `buildAllCardMeshes()`，正交模式下不画面片线框 |

线稿平滑：**不要**对线稿用三次 B 样条（会超调离开墨水）；用 `rdpSimplify` + `resamplePolyline`。

---

## 给下一个 Agent 的工作方式

1. 只改 `app.html`，小步提交到 **`dev`**，不要自行 merge `master`。
2. 改 3D 深度或正交绘制后，用 group1 三视图看正视、侧视；俯视用「信顶面」验证，不要把俯视对不齐当成回归，除非信顶面=100% 时俯视仍不贴。
3. 用户 G 是产品经理，特效师用这工具出 FBX。UI 用中文 toast/状态栏说明「现在该看哪一页」。
4. 回滚用本文 tag，不要 `git push --force` 到 `master`，除非负责人明确要求。
